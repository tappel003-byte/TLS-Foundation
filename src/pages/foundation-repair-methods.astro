---
// Promoted from the -v2 draft to live -- superseded the previous
// foundation-repair-methods.astro (git history has it if it's ever needed).
//
// Foundation Repair Methods ("The Tool Box"). Built from the user-pasted
// code blocks: Ken Burns hero, a fully self-contained page (stats bar,
// 4-step process flow, 8 expandable method cards), and a page-specific
// footer service-area paragraph.
//
// Content check (see CONTENT_TRIM_LOG.md): this page already avoided the
// "same argument restated" problem -- it correctly never imports the
// investigation-first/Sandia GEO argument that belongs to /our-approach.
// The real issue was narrower: 4 of the 8 method cards (Push Piers,
// Micropiles, Compaction Grouting, Foam Injection) had "expand for more"
// bodies that were pure filler, restating the preview in vaguer words
// with no new information -- unlike the Drainage card next to them,
// which names specific real hazards (Mancos Shale, karst, mine
// subsidence). Replaced all 4 with genuine technical distinctions,
// including the wood-frame/push-pier limitation already stated
// elsewhere on the site (index.astro's services list) but missing from
// this page's own dedicated Push Piers card.
//
// Second pass: this page's own copy claimed "eight tools, eight
// different jobs," while Home's services grid and this page's own
// footer teaser both correctly say "six methods" -- Monitoring and
// Custom Solutions aren't repair methods in the same sense (one is
// explicitly choosing not to repair yet, the other is a catch-all for
// what doesn't fit the six). Reworded this page's intro and footer text
// to match the accurate framing instead of inflating the count. Also:
// gave Monitoring and Custom Solutions the `id` anchors and schema
// entries the other six methods already had (they were unlinkable and
// invisible to the page's own structured data); rewrote Helical Piers'
// filler body, which had been missed in the first pass; fixed a
// preview/fit redundancy on the Micropiles card (both said "limited
// access"); and corrected "stabilise" to "stabilize."
//
// Third pass, per Tim: cut the "Common Symptoms" flip-card section
// entirely -- it was a flatter rerun of exactly what the Symptom Guide
// already does properly (same categories: sticking doors, diagonal
// cracks, uneven floors, wall/ceiling gaps, stair-step masonry cracks,
// water pooling), except static instead of interactive and with no path
// to a cause. This page's job is methods, not symptoms. That freed room
// to do what Tim actually asked for: every method card except Drainage
// (already the deepest one) got a second paragraph of real mechanical
// detail -- how depth or capacity is actually determined, not just what
// the method is for. Drainage was left alone; it was already the bar
// the others were being measured against.
import Layout from '../layouts/Layout.astro';

const title = 'Foundation Repair Methods | TLS Foundations New Mexico';
const summary =
  "Eight tools, not one system: helical piers, push piers, and micropiles for deep support; compaction grouting and foam injection for loose soil and voids; drainage correction when moisture is the actual problem; monitoring when repair would be premature; and custom solutions when nothing standard fits. None of these methods are proprietary -- any contractor can offer them. The difference is whether the method gets picked before or after the soil is actually understood. A company that sells one system evaluates every problem through that system; carrying the full toolkit means the method follows the diagnosis instead of the other way around.";
const description =
  'We match the repair method to the problem, not the other way around. Helical piers, push piers, grouting, drainage -- all based on a proper diagnosis.';

const serviceAreaText = `TLS Foundations is a New Mexico foundation repair contractor serving residential and commercial clients across the state. We install helical piers, push piers, and micropiles for deep foundation support. We perform compaction grouting and foam injection for void fill and soil densification. We correct drainage and surface grading when moisture is the problem — which in New Mexico, it usually is. We recommend monitoring when repair is premature. We design custom solutions when the soil profile requires something the standard toolkit doesn't cover. Six repair methods on this page, plus monitoring and custom work for when a standard method isn't the answer. The one we recommend is the one the soil warrants — not the one that generates the largest invoice. A company that sells a system evaluates every problem through the lens of that system. We carry a broad enough toolkit that the method genuinely follows the diagnosis. That is the difference.`;

const methods = [
  { id: 'helical-piers', name: 'Helical Piers', description: 'Steel piers with helical plates, rotated into the ground to reach competent bearing soil. Installed where deeper support is needed for homes with shallow foundations, expansive clay, or variable soil conditions.' },
  { id: 'push-piers', name: 'Push Piers', description: 'Steel piers driven into the ground using the weight of the structure as reaction force. Used for commercial buildings, multi-storey homes, and heavy structures where a driven deep-support system is the right approach.' },
  { id: 'micropiles', name: 'Micropiles', description: 'Small-diameter drilled and grouted deep foundation elements. Used where access is limited or the soil profile requires reaching bearing at significant depth through hard material.' },
  { id: 'compaction-grouting', name: 'Compaction Grouting & Mud Jacking', description: 'Low-slump grout injected under pressure -- into the soil to densify it (compaction grouting), or beneath a slab to lift it (mud jacking). Used for loose or collapsible soil that has lost density, or a slab that needs lifting without a full structural repair.' },
  { id: 'foam-injection', name: 'Foam Injection', description: 'Expanding polyurethane foam injected beneath slabs to lift and fill light voids. Used for sunken driveways, sidewalks, light interior slabs, or small voids under concrete.' },
  { id: 'drainage-correction', name: 'Drainage and Grading Correction', description: "Correcting grading, gutters, and surface drainage to direct water away from the foundation. Often the correct first intervention, since most New Mexico foundation movement is moisture-driven -- though it has real limits once soil has already lost bearing capacity or the cause is geological." },
  { id: 'monitoring', name: 'Monitoring', description: 'Structured observation of movement over time to determine whether it is active, stable, or seasonal, before recommending a repair.' },
  { id: 'custom-solutions', name: 'Custom Solutions', description: 'A repair built around the specific site and structure when soil conditions or building type fall outside the standard methods.' },
];

const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'ItemList',
    name: 'Foundation Repair Methods',
    itemListElement: methods.map((m, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      item: {
        '@type': 'Service',
        name: m.name,
        description: m.description,
        url: `https://www.tlsfoundations.com/foundation-repair-methods#${m.id}`,
        provider: { '@type': 'LocalBusiness', name: 'TLS Foundations' },
        areaServed: { '@type': 'State', name: 'New Mexico' },
        serviceType: 'Foundation repair',
      },
    })),
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://www.tlsfoundations.com' },
      { '@type': 'ListItem', position: 2, name: 'Foundation Repair Methods', item: 'https://www.tlsfoundations.com/foundation-repair-methods' },
    ],
  },
];
---

<Layout
  title={title}
  description={description}
  footer="full"
  schema={schema}
  ogImage="/images/foundation-repair-methods-hero.jpg"
  summary={summary}
  pageName="Foundation Repair Methods"
  nextPage={{ path: '/new-mexico-soil-conditions', name: 'New Mexico Soil Conditions' }}
  footerProps={{
    serviceAreaLabel: "Six methods (not to mention a couple others we have up our sleeves). The right one depends on your soil.",
    serviceAreaText,
  }}
>
  <div class="tls-hero">
    <img class="tls-hero-photo" src="/images/foundation-repair-methods-hero.jpg" alt="Foundation repair tools and equipment" fetchpriority="high" />
    <div class="tls-hero-overlay"></div>
    <div class="tls-hero-wrap">
      <div class="tls-hero-box">
        <span class="tls-hero-headline">The tool box</span>
        <span class="tls-hero-sub">(Using the right tool makes all the difference.)</span>
      </div>
    </div>
  </div>

  <div class="tlsfrm">
    <header class="hero">
      <div class="tlsfrm-container">
        <span class="hero-eyebrow">Foundation Repair Methods</span>
        <h1>We start with the ground. <em>The method follows.</em></h1>
        <p>New Mexico soils do not behave the same from one property to the next. We do not force a repair method first and explain it later. We look at what is actually happening, then choose the method that fits the problem.</p>
        <a href="tel:5059914180" class="hero-phone">(505) 991-4180</a>
        <span class="hero-note">You reach the owner</span>
      </div>
    </header>

    <div class="stats-bar reveal">
      <div class="tlsfrm-container">
        <div class="stat-item">
          <span class="stat-num" data-target="1000000">0<span>+</span></span>
          <span class="stat-label">Miles across New Mexico</span>
        </div>
        <div class="stat-connector">We didn't read about this ground. We drove it. <em>And still driving</em> — we wouldn't have it any other way.</div>
        <div class="stat-item">
          <span class="stat-num" data-target="440000">0</span>
          <span class="stat-label">Field miles on one truck</span>
        </div>
      </div>
    </div>

    <div class="gold-rule" aria-hidden="true"></div>

    <section class="section" id="how-we-decide">
      <div class="tlsfrm-container">
        <div class="section-label reveal">
          <span class="section-label-bar" aria-hidden="true"></span>
          <span class="section-label-text">How we decide</span>
        </div>
        <h2 class="reveal">A repair is only as good as <em>the judgment behind it.</em></h2>
        <p class="reveal">We are not looking for a reason to install a product. We are looking for the reason the structure moved in the first place. Sometimes the right answer is a foundation repair method. Sometimes it is drainage. Sometimes it is monitoring. Sometimes it is a combination of those things.</p>

        <div class="process-flow reveal">
          <div class="process-step">
            <span class="step-number">Step 1</span>
            <div class="step-title">Understand the problem</div>
            <p class="step-desc">A floor-level survey, historic data, and what 55 combined years in New Mexico soils already tells us about an area — Santa Fe leans expansive clay, Los Lunas leans collapsible. Not secret, just experience.</p>
          </div>
          <span class="process-arrow">→</span>
          <div class="process-step">
            <span class="step-number">Step 2</span>
            <div class="step-title">Find the cause</div>
            <p class="step-desc">Is water pooling near the foundation? A plumbing leak? Seasonal drying? We trace the movement back to its source as far as the evidence allows — sometimes it's clear-cut, sometimes it isn't fully knowable, and we say so rather than guess.</p>
          </div>
          <span class="process-arrow">→</span>
          <div class="process-step">
            <span class="step-number">Step 3</span>
            <div class="step-title">Select the method</div>
            <p class="step-desc">We match the repair to the soil, the structure, and the budget. Each method has a specific use.</p>
          </div>
          <span class="process-arrow">→</span>
          <div class="process-step">
            <span class="step-number">Step 4</span>
            <div class="step-title">Verify and monitor</div>
            <p class="step-desc">We check that the repair is working. Sometimes that means monitoring over time. We stay with the job until it's stable.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section" id="methods">
      <div class="tlsfrm-container">
        <div class="section-label reveal">
          <span class="section-label-bar" aria-hidden="true"></span>
          <span class="section-label-text">Our methods</span>
        </div>
        <h2 class="reveal">The method follows the problem. <em>These are the tools.</em></h2>
        <p class="reveal">Six repair methods below, plus two calls that aren't repairs at all — knowing when to wait, and knowing when nothing standard fits. Click or tap any one to see what it's actually for.</p>

        <div class="methods-grid reveal">
          <div class="method-card" id="helical-piers" role="button" tabindex="0" aria-label="Expand to learn more about Helical Piers">
            <div class="method-header">
              <span class="method-number">01</span>
              <div>
                <div class="method-name">Helical Piers</div>
                <span class="method-tag">Deep bearing · Residential</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Installed where deeper support is needed and where site conditions call for a deep foundation solution.</p>
            <div class="method-body">
              <p>Rotated in by torque, not driven by the structure's weight or drilled through rock — which is exactly why it's the default choice for most New Mexico residential work: no dead-load requirement like a push pier, no drill rig needed like a micropile.</p>
              <p>Depth isn't decided in advance. During installation we track the torque required to keep turning the pier, which correlates directly to the soil's bearing capacity — when the numbers say it's reached competent ground, that's the depth, not a number picked off a chart beforehand.</p>
              <div class="method-fit"><strong>When it fits:</strong> Homes with shallow foundations, expansive clay, or variable soil conditions.</div>
              <a href="/helical-piers-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="push-piers" role="button" tabindex="0" aria-label="Expand to learn more about Push Piers">
            <div class="method-header">
              <span class="method-number">02</span>
              <div>
                <div class="method-name">Push Piers</div>
                <span class="method-tag">Heavy structures · Settlement</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Used when the structure and site conditions call for a driven deep support system.</p>
            <div class="method-body">
              <p>Push piers rely on the structure's own weight to seat them, which is exactly why they're the right call for heavy commercial buildings and the wrong call for most New Mexico wood-frame homes — there isn't enough dead load to drive them properly.</p>
              <p>Depth varies pier to pier on the same building, too. Each one only goes as deep as the dead load directly above that specific bracket allows — which is also exactly why an under-loaded structure can't drive a full set of them to consistent depth.</p>
              <div class="method-fit"><strong>When it fits:</strong> Commercial buildings, multi-storey homes, or heavy structures where driven support is the right approach.</div>
              <a href="/push-piers-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="micropiles" role="button" tabindex="0" aria-label="Expand to learn more about Micropiles">
            <div class="method-header">
              <span class="method-number">03</span>
              <div>
                <div class="method-name">Micropiles</div>
                <span class="method-tag">Drilled · Deep profiles</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">The drilled option for hard material a driven or torqued pier can't get through.</p>
            <div class="method-body">
              <p>Drilled and grouted rather than driven or torqued in, which is what makes them work where hard rock, cobbles, or dense caliche would stop a helical pier cold.</p>
              <p>Most of their capacity comes from friction along the grouted shaft, not from bearing at the tip the way a helical or push pier works — which is part of why they can still carry real structural load through material too broken up or too hard for either of those to find a clean stopping point in.</p>
              <div class="method-fit"><strong>When it fits:</strong> Tight spaces, deep unstable soil, or limited access where other equipment can't reach.</div>
              <a href="/micropiles-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="compaction-grouting" role="button" tabindex="0" aria-label="Expand to learn more about Compaction Grouting and Mud Jacking">
            <div class="method-header">
              <span class="method-number">04</span>
              <div>
                <div class="method-name">Compaction Grouting &amp; Mud Jacking</div>
                <span class="method-tag">Loose soils · Slab lifting</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Same grout, two different jobs — one densifies the soil, one lifts a slab from beneath it.</p>
            <div class="method-body">
              <p>Compaction grouting densifies loose or collapsible soil under pressure — a different job than lifting a slab, and the reason it's a different tool than foam injection. Mud jacking uses that same material, aimed at filling a void beneath a slab instead, to lift it — the same goal as foam injection, just a different material with a different cost and cure time.</p>
              <p>Injected through a grid of small-diameter holes, low-slump enough that it displaces and compacts the soil around it instead of just flowing into it. We watch for slight surface heave during injection — that's the signal the ground underneath is actually densifying, not just absorbing grout.</p>
              <div class="method-fit"><strong>When it fits:</strong> Loose or collapsible soil that's lost density, or a slab that needs lifting without a full structural repair.</div>
              <a href="/compaction-grouting-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="foam-injection" role="button" tabindex="0" aria-label="Expand to learn more about Foam Injection">
            <div class="method-header">
              <span class="method-number">05</span>
              <div>
                <div class="method-name">Foam Injection</div>
                <span class="method-tag">Slab leveling · Light voids</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Often used for slab lifting and void filling where the movement is more about the slab than the larger foundation system.</p>
            <div class="method-body">
              <p>Lifts by expansion force, which is plenty for a sidewalk or a light slab but not a substitute for piers under a structure carrying real structural load.</p>
              <p>It cures in minutes, not days, which is what makes it good for precise, incremental lifting — inject a little, check the level, inject a little more, walk on it the same day. That speed is also its ceiling: there's no practical way to use it for a slow, controlled lift under real structural weight.</p>
              <div class="method-fit"><strong>When it fits:</strong> Sunken driveways, sidewalks, light interior slabs, or small voids under concrete.</div>
              <a href="/foam-injection-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="drainage-correction" role="button" tabindex="0" aria-label="Expand to learn more about Drainage and Grading">
            <div class="method-header">
              <span class="method-number">06</span>
              <div>
                <div class="method-name">Drainage and Grading</div>
                <span class="method-tag">Often the right first step</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Sometimes the right repair is not a structural repair at all.</p>
            <div class="method-body">
              <p>The majority of foundation movement in New Mexico is moisture-driven, so correcting drainage and grading is often the most important step on a property — and the correct first intervention, since a structural repair doesn't change what the water is doing. It has real limits, though: once soil has already lost its bearing capacity, or the cause is geological — Mancos Shale heave, karst voids in the Pecos Valley, mine subsidence in the Raton Basin — drainage correction alone won't fix it.</p>
              <div class="method-fit"><strong>When it fits:</strong> Water is the obvious culprit — downspouts, grading, irrigation, or surface runoff — and the soil hasn't yet lost its ability to bear load.</div>
              <a href="/drainage-correction-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="monitoring" role="button" tabindex="0" aria-label="Expand to learn more about Monitoring">
            <div class="method-header">
              <span class="method-number">07</span>
              <div>
                <div class="method-name">Monitoring</div>
                <span class="method-tag">When waiting is right</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Not every issue needs immediate intervention.</p>
            <div class="method-body">
              <p>In some cases, the smartest move is to watch the movement over time so we can understand whether it is active, stable, or related to a seasonal condition.</p>
              <p>In practice that means crack monitors or measurements at fixed points, tracked over weeks or months rather than a single visit — enough to tell active, worsening movement from something that already found its resting point.</p>
              <div class="method-fit"><strong>When it fits:</strong> Minor movement that may stabilize on its own, or when we need more data before acting.</div>
              <a href="/monitoring-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>

          <div class="method-card" id="custom-solutions" role="button" tabindex="0" aria-label="Expand to learn more about Custom Solutions">
            <div class="method-header">
              <span class="method-number">08</span>
              <div>
                <div class="method-name">Custom Solutions</div>
                <span class="method-tag">When nothing else fits</span>
              </div>
              <span class="method-expand-icon">+</span>
            </div>
            <p class="method-preview">Some sites do not fit neatly into a standard category.</p>
            <div class="method-body">
              <p>When that happens, we build the repair around the actual conditions instead of forcing the problem into a template.</p>
              <p>Historic adobe with no standard bearing reference to design against, an addition built on a different foundation type than the original structure, a site with utility conflicts or contaminated soil that rules out standard equipment — the common thread is that none of the other seven methods were built with that specific problem in mind.</p>
              <div class="method-fit"><strong>When it fits:</strong> Unique structures, historic buildings, complex site conditions, or anything that doesn't fit a standard method.</div>
              <a href="/custom-solutions-deep-dive" class="method-deep-link">Read the full technical breakdown →</a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</Layout>

<script>
  function initMethodsPage() {
    const flow = document.querySelector('.process-flow');
    if (flow) {
      const steps = Array.from(flow.querySelectorAll<HTMLElement>('.process-step'));
      let walked = false;
      function walk() {
        if (walked) return;
        walked = true;
        let i = 0;
        function light() {
          steps.forEach((s) => s.classList.remove('active'));
          if (i < steps.length) {
            steps[i].classList.add('active');
            i++;
            setTimeout(light, 1500);
          }
        }
        light();
      }
      const flowObs = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              walk();
              flowObs.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.4 }
      );
      flowObs.observe(flow);
    }

    const revealObs = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            revealObs.unobserve(entry.target);
          }
        });
      },
      { rootMargin: '0px 0px -60px 0px' }
    );
    document.querySelectorAll('.reveal').forEach((el) => revealObs.observe(el));

    function animateCount(el: HTMLElement) {
      const target = parseInt(el.dataset.target || '0', 10);
      if (!target) return;
      const suffixEl = el.querySelector('span');
      const suffix = suffixEl ? suffixEl.outerHTML : '';
      const duration = 3000;
      const start = performance.now();
      function update(now: number) {
        const p = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - p, 3);
        const val = Math.floor(eased * target);
        el.innerHTML = val.toLocaleString() + suffix;
        if (p < 1) requestAnimationFrame(update);
      }
      requestAnimationFrame(update);
    }
    const statsBar = document.querySelector('.stats-bar');
    if (statsBar) {
      const statObs = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.querySelectorAll<HTMLElement>('.stat-num[data-target]').forEach(animateCount);
              statObs.unobserve(entry.target);
            }
          });
        },
        { rootMargin: '0px 0px -80px 0px' }
      );
      statObs.observe(statsBar);
    }

    document.querySelectorAll<HTMLElement>('.method-card').forEach((card) => {
      card.addEventListener('click', () => card.classList.toggle('expanded'));
      card.addEventListener('keydown', (e) => {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          card.classList.toggle('expanded');
        }
      });
    });
  }
  document.addEventListener('DOMContentLoaded', initMethodsPage);
</script>

<style>
  /* ===== TLS KEN BURNS HERO ===== */
  .tls-hero {
    position: relative;
    width: 100%;
    min-height: 560px;
    overflow: hidden;
    display: flex;
    align-items: flex-end;
  }
  .tls-hero-photo {
    position: absolute;
    inset: -10%;
    width: 120%;
    height: 120%;
    object-fit: cover;
    object-position: center;
    animation: tlsKenBurns 20s ease-in-out infinite alternate;
  }
  @keyframes tlsKenBurns {
    from {
      transform: scale(1) translate(0, 0);
    }
    to {
      transform: scale(1.08) translate(-1%, -1%);
    }
  }
  .tls-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(13, 34, 56, 0.55) 0%, rgba(13, 34, 56, 0.25) 45%, rgba(13, 34, 56, 0.1) 100%);
  }
  .tls-hero-wrap {
    position: relative;
    z-index: 2;
    width: 100%;
    padding: 0 clamp(20px, 5vw, 80px) clamp(28px, 5vw, 64px);
  }
  .tls-hero-box {
    display: inline-block;
    background: rgba(13, 34, 56, 0.55);
    padding: 28px 40px 32px;
  }
  .tls-hero-headline {
    font-family: 'Playfair Display', serif;
    font-size: clamp(36px, 6vw, 72px);
    font-weight: 400;
    color: #fff;
    line-height: 1.15;
    display: block;
  }
  .tls-hero-sub {
    font-family: 'Source Serif 4', serif;
    font-size: clamp(15px, 1.8vw, 18px);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.85);
    font-style: italic;
    margin-top: 12px;
    display: block;
  }

  /* ===== page tokens ===== */
  .tlsfrm {
    --navy: #0d2238;
    --blue: #1b3a5c;
    --gold: #b8860b;
    /* gold-text: darkened gold for text on cream/sand/white -- --gold itself only clears WCAG AA on dark backgrounds */
    --gold-text: #7d5c08;
    --stone: #ddd5c8;
    --stone-warm: #e8dfce;
    --cream-label: #ece0c8;
    --slate: #2c3a4a;
    --muted: #54626f;
    --cream: #fdfaf6;
    --white: #ffffff;
    font-family: 'Source Serif 4', Georgia, serif;
    background: var(--cream);
    color: var(--slate);
    line-height: 1.6;
  }
  .tlsfrm-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px;
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }
  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .gold-rule {
    height: 2px;
    background: linear-gradient(to right, var(--gold), rgba(184, 134, 11, 0.08));
    margin: 0;
  }

  .hero {
    padding: 80px 0 60px;
    background: var(--navy);
    position: relative;
    overflow: hidden;
  }
  .hero::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(to right, var(--gold), rgba(184, 134, 11, 0.2));
  }
  .hero .tlsfrm-container {
    position: relative;
    z-index: 2;
  }
  .hero-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    letter-spacing: 5px;
    text-transform: uppercase;
    color: var(--gold);
    display: block;
    margin-bottom: 16px;
  }
  .hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(40px, 6vw, 72px);
    font-weight: 700;
    color: var(--white);
    line-height: 1.05;
    letter-spacing: -1.5px;
    margin-bottom: 16px;
  }
  .hero h1 em {
    font-style: italic;
    color: var(--gold);
  }
  .hero p {
    font-size: clamp(17px, 1.4vw, 20px);
    font-weight: 300;
    color: rgba(255, 255, 255, 0.8);
    max-width: 560px;
    line-height: 1.7;
    margin-bottom: 28px;
  }
  .hero-phone {
    font-family: 'Playfair Display', serif;
    font-size: clamp(24px, 2.8vw, 32px);
    font-weight: 400;
    color: var(--white);
    text-decoration: none;
    border-bottom: 2px solid rgba(184, 134, 11, 0.5);
    padding-bottom: 6px;
    transition: color 0.25s, border-color 0.25s;
    display: inline-block;
  }
  .hero-phone:hover {
    color: var(--gold);
    border-color: var(--gold);
  }
  .hero-note {
    font-family: 'DM Mono', monospace;
    font-size: 12px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--gold);
    display: block;
    margin-top: 8px;
  }

  .stats-bar {
    background: var(--white);
    border-bottom: 1px solid var(--stone);
    padding: 32px 0;
  }
  .stats-bar .tlsfrm-container {
    display: grid;
    grid-template-columns: 1fr 1.25fr 1fr;
    gap: 0;
    align-items: center;
  }
  .stat-item {
    padding: 0 48px;
    text-align: center;
  }
  .stat-item:first-child {
    padding-left: 0;
    text-align: left;
  }
  .stat-item:last-child {
    padding-right: 0;
    text-align: right;
  }
  .stat-num {
    font-family: 'Playfair Display', serif;
    font-size: clamp(32px, 4vw, 48px);
    font-weight: 700;
    color: var(--navy);
    line-height: 1;
    letter-spacing: -1px;
    display: block;
  }
  .stat-num span {
    color: var(--gold-text);
  }
  .stat-label {
    font-family: 'DM Mono', monospace;
    font-size: 11px;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    color: var(--muted);
    display: block;
    margin-top: 6px;
  }
  .stat-connector {
    font-family: 'Playfair Display', serif;
    font-style: italic;
    font-size: clamp(15px, 1.4vw, 18px);
    font-weight: 400;
    color: var(--navy);
    line-height: 1.5;
    text-align: center;
    max-width: 320px;
    justify-self: center;
    padding: 0 24px;
    border-left: 1px solid var(--stone);
    border-right: 1px solid var(--stone);
  }
  .stat-connector em {
    font-style: italic;
    color: var(--gold-text);
  }

  .section {
    padding: 80px 0;
    border-bottom: 1px solid var(--stone);
    background: var(--cream);
  }
  .section:last-of-type {
    border-bottom: none;
  }
  .section-label {
    display: flex;
    align-items: center;
    gap: 18px;
    margin-bottom: 24px;
  }
  .section-label-bar {
    width: 44px;
    height: 2px;
    background: var(--gold);
    flex-shrink: 0;
  }
  .section-label-text {
    font-family: 'DM Mono', monospace;
    font-size: 12px;
    letter-spacing: 5px;
    text-transform: uppercase;
    color: var(--gold-text);
  }
  .section h2 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(28px, 3.6vw, 42px);
    font-weight: 700;
    color: var(--navy);
    line-height: 1.15;
    letter-spacing: -0.5px;
    margin-bottom: 24px;
    max-width: 680px;
  }
  .section h2 em {
    font-style: italic;
    color: var(--gold-text);
  }
  .section > .tlsfrm-container > p {
    font-size: 18px;
    color: var(--slate);
    line-height: 1.9;
    max-width: 580px;
    margin-bottom: 32px;
  }

  .process-flow {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr;
    gap: 0;
    align-items: stretch;
    margin-top: 8px;
  }
  .process-step {
    position: relative;
    padding: 26px 22px;
    background: var(--navy);
    border-radius: 4px;
    border-top: 2px solid rgba(184, 134, 11, 0.25);
    transform: translateY(0);
    transition: background 0.6s ease, transform 0.6s ease, box-shadow 0.6s ease, border-color 0.6s ease;
  }
  .process-step.active {
    background: var(--stone-warm);
    transform: translateY(-6px);
    border-top-color: var(--gold-text);
    box-shadow: 0 14px 34px rgba(13, 34, 56, 0.28);
  }
  .process-step .step-number {
    font-family: 'DM Mono', monospace;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 2px;
    color: var(--cream-label);
    display: block;
    margin-bottom: 6px;
    transition: color 0.6s ease;
  }
  .process-step.active .step-number {
    color: #8a6d0b;
  }
  .process-step .step-title {
    font-family: 'Playfair Display', serif;
    font-size: 18px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 6px;
    transition: color 0.6s ease;
  }
  .process-step.active .step-title {
    color: var(--navy);
  }
  .process-step .step-desc {
    font-size: 15px;
    color: rgba(255, 255, 255, 0.9);
    line-height: 1.6;
    margin: 0;
    transition: color 0.6s ease;
  }
  .process-step.active .step-desc {
    color: var(--slate);
  }
  .process-arrow {
    font-family: 'DM Mono', monospace;
    font-size: 20px;
    color: var(--gold-text);
    padding: 0 12px;
    opacity: 0.4;
    align-self: center;
  }

  .methods-grid {
    /* Single column, not a multi-column grid -- a grid's row height is
       shared across every card in that row (default align-items:
       stretch), so the moment one card expands, its unexpanded
       row-mates stretch to match and show a block of dead space inside
       their own border. That's structural to any layout that puts
       variable-height cards side by side, not fixable by tweaking
       alignment -- the only real fix is never sharing a row. */
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 8px;
  }
  .method-card {
    background: var(--white);
    border: 1px solid var(--stone);
    border-radius: 4px;
    overflow: hidden;
    transition: border-color 0.3s, box-shadow 0.3s;
    cursor: pointer;
  }
  .method-card:hover {
    border-color: rgba(184, 134, 11, 0.3);
    box-shadow: 0 4px 20px rgba(13, 34, 56, 0.04);
  }
  .method-card.expanded {
    border-color: var(--gold);
    box-shadow: 0 8px 32px rgba(13, 34, 56, 0.06);
  }
  .method-header {
    padding: 24px 24px 16px;
    display: flex;
    align-items: flex-start;
    gap: 16px;
  }
  .method-number {
    font-family: 'DM Mono', monospace;
    font-size: 14px;
    letter-spacing: 2px;
    color: var(--gold-text);
    flex-shrink: 0;
    margin-top: 2px;
    opacity: 0.5;
  }
  .method-name {
    font-family: 'Playfair Display', serif;
    font-size: 20px;
    font-weight: 700;
    color: var(--navy);
    line-height: 1.2;
  }
  .method-tag {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 1.5px;
    color: var(--gold-text);
    display: block;
    margin-top: 2px;
    opacity: 0.6;
  }
  .method-preview {
    padding: 0 24px 16px;
    font-size: 15px;
    color: var(--slate);
    line-height: 1.6;
    margin: 0;
  }
  .method-expand-icon {
    font-family: 'DM Mono', monospace;
    font-size: 18px;
    color: var(--gold-text);
    margin-left: auto;
    flex-shrink: 0;
    opacity: 0.5;
    transition: transform 0.3s;
  }
  .method-card.expanded .method-expand-icon {
    transform: rotate(45deg);
  }
  .method-body {
    display: none;
    padding: 0 24px 24px;
    border-top: 1px solid var(--stone);
    margin-top: 4px;
    animation: expandFade 0.3s ease;
  }
  .method-card.expanded .method-body {
    display: block;
  }
  @keyframes expandFade {
    from {
      opacity: 0;
      transform: translateY(-6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .method-body p {
    font-size: 15px;
    color: var(--slate);
    line-height: 1.7;
    margin: 16px 0 0 0;
    max-width: none;
  }
  .method-body .method-fit {
    font-size: 14px;
    color: var(--navy);
    background: var(--cream);
    padding: 12px 16px;
    border-radius: 3px;
    margin-top: 16px;
  }
  .method-deep-link {
    display: inline-block;
    margin-top: 14px;
    font-family: 'DM Mono', monospace;
    font-size: 12px;
    letter-spacing: 0.5px;
    color: var(--blue);
    text-decoration: none;
    border-bottom: 1px dotted var(--gold);
    transition: color 0.2s;
  }
  .method-deep-link:hover {
    color: var(--gold-text);
  }
  .method-body .method-fit strong {
    color: var(--gold-text);
    font-weight: 600;
  }

  @media (max-width: 960px) {
    .tlsfrm-container {
      padding: 0 28px;
    }
    .hero {
      padding: 60px 0 48px;
    }
    .section {
      padding: 56px 0;
    }
    .process-flow {
      grid-template-columns: 1fr;
      gap: 16px;
    }
    .process-arrow {
      display: none;
    }
    .stats-bar .tlsfrm-container {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .stat-item {
      padding: 0;
      text-align: center !important;
    }
    .stat-connector {
      max-width: 100%;
      border-left: none;
      border-right: none;
      border-top: 1px solid var(--stone);
      border-bottom: 1px solid var(--stone);
      padding: 20px 0;
    }
  }
  @media (max-width: 480px) {
    .tlsfrm-container {
      padding: 0 20px;
    }
    .hero h1 {
      letter-spacing: -1px;
    }
    .method-header {
      flex-wrap: wrap;
    }
    .method-expand-icon {
      margin-left: 0;
      width: 100%;
      text-align: right;
    }
  }
</style>
