---
// Fact-forward pass (matches the corrected helical-piers-deep-dive.astro
// treatment). Covers both compaction grouting and mud jacking, per Tim:
// same grout material, aimed differently (into the soil to densify it, or
// under a slab to lift it). Re-verified against MFRC's compaction-grouting
// page and rewritten in TLS's own structure. Added the physical-connection
// vs. ground-improvement framing Tim asked for: piering makes a physical
// connection to bearing material, grouting improves the ground itself --
// physical connection is usually the stronger repair when it's achievable,
// ground improvement is the right call, not a fallback, when it isn't.
// No misconceptions/Q&A section (dropped sitewide). [TIM: ...] marks
// spots needing real NM numbers/field experience.
import Layout from '../layouts/Layout.astro';

const title = 'Compaction Grouting & Mud Jacking in New Mexico | TLS Foundations';
const summary =
  'Compaction grouting and mud jacking use the same stiff, low-mobility grout, aimed at two different jobs -- injected into the soil itself to displace and densify it, or injected beneath a slab to fill a void and lift it. Neither creates a pile; neither is foam. Compaction grouting is ground improvement, not a shortcut around understanding the ground -- it depends entirely on soil that can actually take displacement without fracturing or migrating somewhere it shouldn\'t.';
const description =
  'What compaction grouting and mud jacking actually are, how the same grout does two different jobs, and when New Mexico soil conditions call for one over the other.';

const schema = [
  {
    '@context': 'https://schema.org',
    '@type': 'Article',
    headline: 'Compaction Grouting & Mud Jacking in New Mexico',
    description,
    about: description,
    author: { '@type': 'Organization', name: 'TLS Foundations' },
    publisher: { '@type': 'Organization', name: 'TLS Foundations' },
    url: 'https://www.tlsfoundations.com/compaction-grouting-deep-dive',
  },
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://www.tlsfoundations.com' },
      { '@type': 'ListItem', position: 2, name: 'Foundation Repair Methods', item: 'https://www.tlsfoundations.com/foundation-repair-methods' },
      { '@type': 'ListItem', position: 3, name: 'Compaction Grouting & Mud Jacking', item: 'https://www.tlsfoundations.com/compaction-grouting-deep-dive' },
    ],
  },
];
---

<Layout
  title={title}
  description={description}
  footer="minimal"
  schema={schema}
  summary={summary}
  pageName="Compaction Grouting & Mud Jacking"
  nextPage={{ path: '/foundation-repair-methods', name: 'Foundation Repair Methods' }}
>
  <div class="tls-article" id="top">
    <section class="page-hero">
      <div class="page-hero-inner">
        <span class="hero-eyebrow">Methods · Compaction Grouting &amp; Mud Jacking</span>
        <h1>Compaction Grouting &amp; Mud Jacking —<br /><em>Same Grout, Two Jobs</em></h1>
        <p class="page-hero-sub">Ground improvement by displacement, not a pile and not foam. One material, aimed at the soil itself or aimed under a slab, depending on which problem you actually have.</p>
      </div>
    </section>

    <div class="article-outer">
      <article class="article-body">
        <p class="article-lede">"Grouting" gets used loosely enough on estimates that it stops meaning anything specific. This page is about the one mechanism that actually matters: a stiff, low-mobility grout that displaces the material around it instead of soaking into it.</p>

        <div class="article-section" id="what-they-are">
          <h2>What They <em>Actually Are</em></h2>
          <p class="opening">Both compaction grouting and mud jacking use a stiff, low-slump, low-mobility grout — meant to stay near the injection point and form a bulb, or a string of bulbs, rather than soak into ordinary soil pores the way a thinner grout would.</p>
          <p>The difference is entirely about where it's aimed. Compaction grouting injects into the soil itself — as each bulb grows under controlled pressure, it displaces the ground around it, and in suitable material that displacement increases density and stiffness. Mud jacking uses the same material but aimed beneath a slab instead, filling a void to restore contact and lift the concrete. Same grout, two different targets, two different jobs.</p>
        </div>

        <div class="article-section" id="how-they-work">
          <h2>How They Actually <em>Work</em></h2>
          <p class="opening">Neither of these creates a conventional pile that carries building load down to a distant deep layer — they're meant to improve or support what's already there, not replace it with something new.</p>
          <p>That's the real distinction between this method and piering, not a matter of which is "better." A pier makes a physical connection from the footing to competent bearing material — it's usually the stronger repair when that connection is achievable. Grouting doesn't connect to anything; it improves the ground itself. That makes it the right call, not a fallback, when a physical connection isn't achievable or isn't what the soil condition actually calls for.</p>
          <p>For compaction grouting specifically, what you get depends on the soil itself, the treatment depths and pattern, the injection sequence, pressure and volume control, and monitoring throughout. It's ground improvement, not "adding concrete underground" as a slogan — the mechanism is displacement and densification of material that's already lost density, and uncontrolled volume can lift or fracture more than the target zone, which is why planned, monitored injection is the whole point rather than an open-ended pour.</p>
          <div class="callout-block">
            <span class="callout-label">Not the same tool as foam</span>
            <p>Compaction grouting is a different tool from foam injection, even though both can be used to address a settled slab. Foam is an expanding polyurethane resin that fills a void; compaction grout is a stiff cement-based material that displaces and compacts the ground around it. They're not interchangeable, and a bid that just says "grouting" or "soil stabilization" hasn't actually named a method — ask what mechanism is planned.</p>
          </div>
        </div>

        <div class="article-section" id="scope">
          <h2>What Rules Either One <em>Out</em></h2>
          <p class="opening">An active, unaddressed leak, roof runoff, grading, or irrigation problem still driving the movement.</p>
          <p>A foundation that actually needs a defined deep load path rather than improved shallow or intermediate support. Nearby foundations, utilities, and slabs that haven't been accounted for — displacement can move more than just the target zone.</p>
        </div>

        <div class="article-section" id="installation">
          <h2>What Installation <em>Actually Involves</em></h2>
          <p class="opening">Both are injected through a grid of small-diameter holes at planned locations, depths, and stages, with volume and pressure controlled and monitored throughout — not a single injection point, and not an open-ended pour.</p>
          <p>[TIM: how often does this actually come up in your NM work, and what does a typical job look like when it does — equipment, timeline, anything homeowners should expect that's different from the piering jobs?]</p>
        </div>

        <a href="#top" class="back-to-top">↑ Back to top</a>

        <p class="closing-link"><a href="/foundation-repair-methods" class="body-link">← Back to Foundation Repair Methods</a> · <a href="/glossary#compaction-grouting" class="body-link">Glossary: Compaction Grouting →</a></p>
      </article>
    </div>
  </div>
</Layout>

<style>
  .tls-article {
    --blue: #1b3a5c;
    --blue-dark: #0d2238;
    --blue-mid: #2a5480;
    --tls-gold: #b8860b;
    --gold-light: #d4a017;
    --gold-text: #7d5c08;
    --tls-sand: #f5efe6;
    --tls-stone: #e0d5c5;
    --tls-slate: #4a5568;
    --muted: #54626f;
    --dark: #1a1a1a;
    --tls-cream: #fdfaf6;
    --gutter: 64px;
    --content-max: 1100px;
    --article-max: 720px;
    font-family: 'Source Serif 4', Georgia, serif;
    font-size: 18px;
    background: #fff;
    color: var(--dark);
    line-height: 1.9;
  }
  .page-hero {
    background: var(--blue);
    padding: 52px var(--gutter) 44px;
    border-bottom: 2px solid var(--tls-gold);
  }
  .page-hero-inner {
    max-width: var(--content-max);
    margin: 0 auto;
  }
  .hero-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 10px;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: var(--gold-light);
    display: block;
    margin-bottom: 16px;
  }
  .page-hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(28px, 4vw, 46px);
    font-weight: 400;
    color: #fff;
    line-height: 1.15;
    margin-bottom: 16px;
  }
  .page-hero h1 em {
    font-style: italic;
    color: var(--gold-light);
  }
  .page-hero-sub {
    font-size: 17px;
    font-weight: 300;
    color: rgba(255, 255, 255, 0.5);
    line-height: 1.8;
    max-width: 600px;
  }
  .article-outer {
    max-width: var(--content-max);
    margin: 0 auto;
    padding: 64px var(--gutter) 80px;
  }
  .article-body {
    max-width: var(--article-max);
    margin: 0 auto;
  }
  .article-lede {
    font-size: 20px;
    font-weight: 300;
    line-height: 1.85;
    color: var(--tls-slate);
    margin-bottom: 48px;
    padding-bottom: 48px;
    border-bottom: 1px solid var(--tls-stone);
  }
  .article-section {
    margin-bottom: 56px;
    scroll-margin-top: 80px;
  }
  .article-section h2 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(20px, 2.5vw, 26px);
    font-weight: 400;
    color: var(--blue);
    line-height: 1.2;
    margin-bottom: 24px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--tls-stone);
  }
  .article-section h2 em {
    font-style: italic;
    color: var(--gold-text);
  }
  .article-section p {
    font-size: 17px;
    font-weight: 300;
    color: var(--tls-slate);
    line-height: 1.9;
    margin-bottom: 20px;
  }
  .article-section p:last-child {
    margin-bottom: 0;
  }
  .article-section p.opening {
    font-size: 18px;
    color: var(--blue);
    font-weight: 400;
  }
  .pull-quote {
    margin: 40px 0;
    padding: 32px 40px;
    border-left: 3px solid var(--gold-text);
    background: var(--tls-sand);
  }
  .pull-quote p {
    font-family: 'Playfair Display', serif;
    font-size: 20px !important;
    font-weight: 400;
    font-style: italic;
    color: var(--blue) !important;
    line-height: 1.55 !important;
    margin-bottom: 0 !important;
  }
  .callout-block {
    margin: 40px 0;
    padding: 28px 32px;
    background: var(--tls-cream);
    border-top: 2px solid var(--tls-gold);
    border-bottom: 1px solid var(--tls-stone);
  }
  .callout-label {
    font-family: 'DM Mono', monospace;
    font-size: 9px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gold-text);
    display: block;
    margin-bottom: 14px;
  }
  .callout-block p {
    font-size: 15px !important;
    font-weight: 300;
    color: var(--tls-slate) !important;
    line-height: 1.8 !important;
    margin-bottom: 10px !important;
  }
  .callout-block p:last-child {
    margin-bottom: 0 !important;
  }
  .callout-block strong {
    color: var(--blue);
    font-weight: 600;
  }
  .key-points {
    margin: 32px 0;
    list-style: none;
  }
  .key-points li {
    font-size: 16px;
    font-weight: 300;
    color: var(--tls-slate);
    line-height: 1.75;
    padding: 12px 0 12px 24px;
    border-bottom: 1px solid var(--tls-stone);
    position: relative;
  }
  .key-points li:first-child {
    border-top: 1px solid var(--tls-stone);
  }
  .key-points li::before {
    content: '—';
    font-family: 'DM Mono', monospace;
    color: var(--gold-text);
    position: absolute;
    left: 0;
    font-size: 13px;
  }
  .key-points li strong {
    color: var(--blue);
    font-weight: 600;
  }
  .back-to-top {
    display: block;
    text-align: right;
    margin-top: 48px;
    font-family: 'DM Mono', monospace;
    font-size: 9px;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .back-to-top:hover {
    color: var(--gold-text);
  }
  .closing-link {
    margin-top: 32px;
    font-size: 17px;
  }
  .tls-article :global(.body-link) {
    color: var(--blue);
    text-decoration: underline;
    text-decoration-color: var(--tls-gold);
    text-underline-offset: 3px;
    transition: color 0.2s;
  }
  .tls-article :global(.body-link):hover {
    color: var(--gold-text);
  }

  @media (max-width: 768px) {
    .article-outer {
      padding: 40px 24px 56px;
    }
    .pull-quote {
      padding: 24px 24px;
    }
  }
</style>
